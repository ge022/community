<%= form_with(model: forum, url: community_forum_path(:id => @forum.id, :community_id => @community.id), local: true, class: 'needs-validation', html: {novalidate: 'novalidate'}) do |form| %>

  <% if forum.errors.any? %>
    <div id="error_explanation">
      <div><%= pluralize(forum.errors.count, "error") %> prohibited this forum from being saved:</div>
      <ul>
        <% forum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :forum_title, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Title is required.
    </div>
  </div>

  <div class="form-group">
    <%= form.label :post %>
    <%= form.text_area :post, id: :forum_post, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Post content is required.
    </div>
  </div>

  <div class="form-group">
    <%= form.submit "Update", class: "btn btn-primary" %>
  </div>

  <div class="form-group-inline">
    <%= link_to 'Show', community_forum_path(:id => @forum.id), class: "btn btn-secondary" %>
    <%= link_to 'Back', community_forum_path(@community.id), class: "btn btn-secondary" %>
  </div>
<% end %>
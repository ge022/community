<%= form_with(model: [@article, ArticleComment.new], url: community_article_comment_path(@community.id, @article.id), local: true, class: 'needs-validation', html: {novalidate: 'novalidate'}) do |form| %>

  <div class="form-group">
    <%= form.label :comment, class: 'd-none' %>
    <%= form.text_area :comment, id: :reply_post, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Reply content is required.
    </div>
  </div>

  <div class="form-group-inline text-left">
    <%= form.submit "Reply", class: "btn btn-outline-primary" %>
  </div>
<% end %>